<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hjy.gouwu">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".search_home.search_homeActivity" />
        <activity android:name=".Login_register.registre_go" />
        <activity android:name=".Login_register.Login_go" />
        <activity android:name=".UserAddress.Myaddress" />
        <activity android:name=".UserAddress.Add_address" />
        <activity android:name=".search_home.goods_detail.to_goods_detail" />
        <activity android:name=".order.orderActivity"></activity>
    </application>

</manifest>